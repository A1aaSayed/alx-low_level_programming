#include "main.h"
#include <stdlib.h>
/***/

char *str_concat(char *s1, char *s2)
{
	int i, j;
	char *str;
	str = malloc(sizeof(char) * s1 * s2);

	for (i = 0; i <= *s1; i++)
	{
		str 
		for (j = 0; j <= *s2; j++)
		{
			str[i] 
		}
	}
}
