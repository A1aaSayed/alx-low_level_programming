#include "main.h"
#include <stdio.h>

/**
 * print_numbers - print numbers from 1 to 9
 * Return: Always 0
*/

void print_numbers(void)
{
	for (int i = 48; i <= 57; i++)
		_putchar(i);
	_putchar('\n');

	return (0);
}
